# AGENTS.md — 归档记录不可篡改契约

本目录下的 Note 均为已封存的冻结历史快照，不再作为当前代码库的权威依据。

## 不可侵犯禁令

1. 严禁编辑、重写、翻译、重新排版、移动或删除任何已归档的 Note。
2. 严禁为了“修复死链”或“消除过时 API 报错”而修改已归档的文件。
3. 归档文件已被计算 SHA-256 哈希值并封印于 manifest.json 中，任何未经授权的修改都会直接导致门禁报错。
